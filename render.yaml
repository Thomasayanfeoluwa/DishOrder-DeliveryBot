services:
  - type: web
    name: chainlit-app
    runtime: docker
    repo: https://github.com/your-username/your-chainlit-repo
    branch: main
    dockerfilePath: ./Dockerfile
    envVars:
      - key: CHAINLIT_AUTH_SECRET
        generateValue: true
      - key: PORT
        value: 8000