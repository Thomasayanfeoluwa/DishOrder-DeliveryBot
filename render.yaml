services:
  - type: web
    name: dishdash-orderbot
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "chainlit run app.py --host 0.0.0.0 --port $PORT"
    autoDeploy: true
    envVars:
      - key: PORT
        value: "8000"
